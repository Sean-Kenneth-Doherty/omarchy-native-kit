# Incident Credential Rotator

A studio dogfood app for inspecting compromised credential reports, rotation batches, service owners, secret age, rollback snapshots, and post-incident access cleanup.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before `src/styles.css` and keeps visible colors tied to Omarchy theme variables.
