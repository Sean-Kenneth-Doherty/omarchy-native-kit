# DNS Resolver Policy Lab

A studio dogfood app for inspecting resolver routing, split-horizon VPN domains, per-workspace DNS leaks, and rollback-safe DNS changes.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color is driven by Omarchy semantic tokens.
