# Zero Trust Access Review Console

A studio dogfood app for inspecting protected resources, device posture gates, identity risk, stale access exceptions, owner intent, and rollback-safe zero-trust policy changes.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color comes from generated Omarchy theme tokens.
