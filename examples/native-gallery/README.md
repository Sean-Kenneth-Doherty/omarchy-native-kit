# Native Gallery

Native Gallery is an Omarchy-native studio for browsing verified apps and copying blueprint recipes from the local catalog.

```bash
npm install
npm run theme:css
npm run dev
```

It dogfoods the catalog loop:

```bash
omarchy-native app catalog ../../examples
omarchy-native verify .
```
