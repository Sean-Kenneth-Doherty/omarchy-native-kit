# Hardware Token Manager

A studio dogfood app for inspecting FIDO2, PIV, and smartcard devices, enrollment state, touch policies, recovery paths, and rollback-safe token changes.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color is driven by Omarchy semantic tokens.
