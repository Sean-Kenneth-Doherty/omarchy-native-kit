# AppArmor Profile Workbench

A studio dogfood app for inspecting AppArmor profile intents, denied operations, portal alternatives, local overrides, and rollback-safe confinement changes.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color is driven by Omarchy semantic tokens.
