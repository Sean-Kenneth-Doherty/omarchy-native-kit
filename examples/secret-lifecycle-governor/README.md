# Secret Lifecycle Governor

A studio dogfood app for inspecting secret birth, owner attestations, rotation cadence, consumer drift, deletion readiness, and policy-safe lifecycle changes.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before `src/styles.css` and keeps visible colors tied to Omarchy theme variables.
