# Theme Forge

A studio-style dogfood app for inspecting Omarchy semantic tokens and export targets.

```bash
npm install
npm run theme:css
npm run dev
```

Theme Forge demonstrates a tool rail, token canvas, properties panel, export actions, and responsive studio layout built from the `studio` blueprint.
