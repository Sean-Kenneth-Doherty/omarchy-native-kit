# Backup Key Recovery Planner

A studio dogfood app for inspecting recovery keys, backup tokens, emergency passphrases, escrow readiness, and rollback-safe recovery drills.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before `src/styles.css` and keeps visible colors tied to Omarchy theme variables.
