# Agent Context Lab

A dogfood Omarchy-native app for turning theme tokens and agent guidance into a compact build workspace.

```bash
npm install
npm run theme:css
npm run dev
```

The interface uses the generated `src/omarchy-theme.css` file and demonstrates token rows, command surfaces, focus states, status checks, and dense responsive panels.
