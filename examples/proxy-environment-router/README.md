# Proxy Environment Router

A studio dogfood app for inspecting proxy variables, per-app proxy routing, no-proxy gaps, PAC files, and rollback-safe proxy changes.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color is driven by Omarchy semantic tokens.
