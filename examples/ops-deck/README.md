# Ops Deck

Ops Deck is an Omarchy-native dashboard for daily catalog status, release checks, theme rollout tasks, and app operations.

```bash
npm install
npm run theme:css
npm run dev
```

It dogfoods the operator loop:

```bash
npm run catalog:examples
npm run verify:examples
npm pack --dry-run
```
