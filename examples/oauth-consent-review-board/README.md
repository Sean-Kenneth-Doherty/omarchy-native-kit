# OAuth Consent Review Board

A studio dogfood app for inspecting OAuth grants, delegated scopes, stale app access, owner intent, revocation windows, and rollback-safe consent cleanup.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color comes from generated Omarchy theme tokens.
