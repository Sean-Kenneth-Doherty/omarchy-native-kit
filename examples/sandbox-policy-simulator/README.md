# Sandbox Policy Simulator

A studio dogfood app for inspecting sandbox profiles, bind mounts, portal holes, network namespaces, and rollback-safe policy experiments.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color is driven by Omarchy semantic tokens.
