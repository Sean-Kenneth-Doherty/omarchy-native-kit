# SAML Assertion Trust Lab

A studio dogfood app for inspecting SAML apps, assertion attributes, stale identity provider trust, certificate expiry, owner intent, and rollback-safe federation cleanup.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color comes from generated Omarchy theme tokens.
