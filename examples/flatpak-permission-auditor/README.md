# Flatpak Permission Auditor

A studio dogfood app for inspecting Flatpak permissions, filesystem grants, portal use, override drift, and rollback-safe sandbox changes.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color is driven by Omarchy semantic tokens.
