# Firewall Rule Planner

A studio dogfood app for inspecting nftables and ufw intents, per-workspace network exposure, temporary holes, and rollback-safe firewall changes.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color is driven by Omarchy semantic tokens.
