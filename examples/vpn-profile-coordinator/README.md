# VPN Profile Coordinator

A studio dogfood app for inspecting VPN profiles, route ownership, kill-switch posture, workspace bindings, and rollback-safe tunnel changes.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color is driven by Omarchy semantic tokens.
