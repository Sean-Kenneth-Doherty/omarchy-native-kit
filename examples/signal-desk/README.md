# Signal Desk

A dashboard dogfood app built from `omarchy-native agent blueprint --kind dashboard`.

```bash
npm install
npm run theme:css
npm run dev
```

Signal Desk demonstrates metric tiles, activity rows, filters, detail panels, semantic status colors, and keyboard focus states using generated Omarchy theme variables.
