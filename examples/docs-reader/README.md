# Docs Reader

Docs Reader is an Omarchy-native command center for browsing local kit docs, commands, and blueprint guidance.

```bash
npm install
npm run theme:css
npm run dev
```

It dogfoods the reference loop:

```bash
omarchy-native agent prompt
omarchy-native app catalog ../../examples
omarchy-native verify .
```
