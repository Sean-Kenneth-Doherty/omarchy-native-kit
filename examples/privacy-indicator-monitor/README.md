# Privacy Indicator Monitor

A studio dogfood app for inspecting camera, microphone, screen-capture, and location activity indicators, stale grants, and per-workspace privacy posture.

```bash
npm install
npm run theme:css
npm run dev
```

The app imports `src/omarchy-theme.css` before local styles so every visible color is driven by Omarchy semantic tokens.
