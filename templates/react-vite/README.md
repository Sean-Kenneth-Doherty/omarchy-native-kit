# React/Vite Omarchy Native Template

Placeholder for the first app template.

Expected shape:

- generate `src/omarchy-theme.css` with `omarchy-native theme css --out src/omarchy-theme.css`
- import it before app styles
- use semantic CSS variables like `--omarchy-background`, `--omarchy-foreground`, `--omarchy-accent`
- add a theme-set hook to regenerate CSS when Omarchy theme changes
